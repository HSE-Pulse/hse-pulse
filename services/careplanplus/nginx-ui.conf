server {
    listen 5000;
    server_name _;
    absolute_redirect off;
    root /usr/share/nginx/html;
    index index.html;

    # Serve static assets from /careplanplus/ path
    location /careplanplus/ {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /index.html;
    }

    # Health check returns 200, browsers redirect to /careplanplus/
    location = / {
        if ($http_user_agent ~* "GoogleHC") {
            return 200 'OK';
        }
        return 302 /careplanplus/;
    }

    location / {
        try_files $uri $uri/ /careplanplus/index.html;
    }

    location /careplanplus/config.js {
        alias /usr/share/nginx/html/config.js;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    location /careplanplus/api/ {
        proxy_pass http://127.0.0.1:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
